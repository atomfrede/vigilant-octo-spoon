language: java

jdk:
  - oraclejdk8

cache:
  directories:
   - $HOME/.gradle

before_install:
  - jdk_switcher use oraclejdk8
  - java -version

after_success:
  - bash <(curl -s https://codecov.io/bash)